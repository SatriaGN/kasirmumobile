/**
 * sync feature — remote API layer (stub).
 *
 * This project currently runs on in-memory mock data (see `@data/mockData`).
 * When a backend is introduced, put the HTTP calls for the sync domain here and
 * have `services/sync.service.ts` consume them. Keep transport concerns (fetch,
 * headers, DTO mapping) in this file only.
 */
export {};
